// // Logic to fetch the articles

// import { articles } from "../mockData";

// export interface IArticleParams{
//   category: string;
//   id: number;
//   title: string;
//   subtitle: string;
//   content: string;
// }

// export default async function getArticles (params:IArticleParams) {

//   try{
//     const {
//       category,
//       id,
//       title,
//       subtitle,
//       content
//     } = params;

//   // fetch the articles

//   }
//   }catch(error:any){
// throw new Error(error);
//   }
//   // Simulate an asynchronous fetch, e.g., from an API
//   return new Promise((resolve) => {
//     setTimeout(() => {
//       resolve(articles);
//     }, 500); // Simulating a delay, as a real API call might take some time
//   });
// };
